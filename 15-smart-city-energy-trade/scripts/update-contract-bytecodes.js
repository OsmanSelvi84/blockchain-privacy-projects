/**
 * Placeholder: after `yarn compile-contracts`, copy verifier artifact into chain spec
 * following your Parity authority setup (same process as course reference).
 */
console.log(
  "Compile complete. Deploy verifier at VERIFIER_ADDRESS in chain spec, then yarn migrate-contracts-authority"
);
